def fetch_groups(uid):
    return fetch_groups(uid)

def add_group(uid, name):
    return add_group(uid, name)

def delete_group(uid, id):
    return delete_group(uid, id, name)

def rename_group(uid, id, name):
    return rename_group(uid, id, name)
